# Lifeline Engine — Python dependencies
# Install with:  pip install -r requirements.txt

osmnx>=1.9.0
networkx>=3.2
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
pandas>=2.0.0
scipy>=1.12.0             # required by osmnx for nearest-node on projected graphs

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Logging
python-json-logger>=2.0.7  # structured JSON log output (opt-in via LOG_JSON=1)

# Testing / load testing
httpx>=0.27.0              # async HTTP client used by test_load.py
locust>=2.24.0             # sustained load testing (locustfile.py)

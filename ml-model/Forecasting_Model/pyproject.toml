[project]
name = "satellite-insight-engine"
version = "0.1.0"
description = "Flood risk T+24 prediction pipeline with hybrid CNN-LSTM."
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
